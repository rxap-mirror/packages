import { ToDisplayPipe } from './to-display.pipe';

describe('ToDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ToDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
