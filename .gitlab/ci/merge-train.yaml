include:
  - local: .gitlab/ci/jobs/build.yaml

stages:
  - build

variables:
  ENVIRONMENT_NAME: merge-train/$CI_COMMIT_REF_NAME
  DEPLOYMENT_TIER: other

build:
  extends: .build
  environment:
    name: merge-train/$CI_COMMIT_REF_NAME
    deployment_tier: other
