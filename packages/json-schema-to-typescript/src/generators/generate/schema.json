{
  "$schema": "https://json-schema.org/schema",
  "$id": "Generate",
  "title": "",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "Path to the json schema file"
    },
    "output": {
      "type": "string",
      "description": "Path to the output typescript file. Defaults to the filename of the json schema where .json is replaced by .d.ts"
    },
    "suffix": {
      "type": "string",
      "description": "Suffix of the generate interface name"
    }
  },
  "required": ["path"]
}
