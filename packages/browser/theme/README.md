# browser-theme

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build browser-theme` to build the library.

## Running unit tests

Run `nx test browser-theme` to execute the unit tests via [Jest](https://jestjs.io).
