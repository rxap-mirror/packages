{
  "$schema": "http://json-schema.org/schema",
  "$id": "HealthIndicatorInit",
  "title": "",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "For which project should the health indicator module be initialized?"
    }
  },
  "required": [
    "project"
  ]
}
