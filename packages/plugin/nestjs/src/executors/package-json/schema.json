{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "PackageJson executor",
  "description": "",
  "type": "object",
  "properties": {
    "dependencies": {
      "alias": [
        "dependency"
      ],
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "includeLocalProjects": {
      "type": "boolean",
      "default": false,
      "description": "Include local projects in the package.json file, if they have a package.json file"
    }
  },
  "required": []
}
