{
  "executors": {
    "generate": {
      "implementation": "./src/executors/generate/executor",
      "schema": "./src/executors/generate/schema.json",
      "description": "Generate a build.json file with context information about the build environment"
    }
  }
}
