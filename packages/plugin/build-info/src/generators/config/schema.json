{
  "$schema": "http://json-schema.org/schema",
  "$id": "Config",
  "title": "",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "For which project should the generate executor be added?"
    }
  },
  "required": [
    "project"
  ]
}
