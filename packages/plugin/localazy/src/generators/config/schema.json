{
  "$schema": "http://json-schema.org/schema",
  "$id": "Config",
  "title": "",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to add the Localazy builder to.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "For which project should the localazy builder be added?",
      "examples": ["my-app"]
    },
    "extractTarget": {
      "type": "string",
      "description": "The Nx target responsible for extracting or generating the translation source files.",
      "examples": ["extract-i18n"]
    },
    "writeKey": {
      "type": "string",
      "description": "The Localazy project write key. This key will be added to the .env file.",
      "x-prompt": "(optional) Enter the localazy write key. This key will be added to the .env file.",
      "examples": ["your-write-key"]
    },
    "readKey": {
      "type": "string",
      "description": "The Localazy project read key. This key will be added to the .env file.",
      "x-prompt": "(optional) Enter the localazy read key. This key will be added to the .env file.",
      "examples": ["your-read-key"]
    },
    "overwrite": {
      "type": "boolean",
      "description": "Whether to overwrite existing Localazy configuration files.",
      "default": false
    }
  },
  "required": [
    "project"
  ]
}
