{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Deploy executor",
  "description": "",
  "type": "object",
  "properties": {
    "endpoint": {
      "type": "string",
      "description": "The endpoint of the web3 storage service"
    },
    "key": {
      "type": "string",
      "description": "The key used to authenticate with the web3 storage service"
    },
    "proof": {
      "type": "string",
      "description": "The proof used to authenticate with the web3 storage service"
    },
    "rateLimit": {
      "type": "number",
      "description": "The rate limit in seconds of the web3 storage service"
    },
    "buildTarget": {
      "type": "string",
      "description": "The build target from which the output should be deployed"
    }
  },
  "required": []
}
