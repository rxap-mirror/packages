{
  "$schema": "http://json-schema.org/schema",
  "$id": "Init",
  "title": "",
  "type": "object",
  "properties": {
    "project": {
      "type": "string"
    },
    "projects": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "components": {
      "type": "boolean",
      "x-prompt": "Should gitlab ci/cd components be used",
      "default": true,
      "description": "If true, the gitlab ci will be initialized with components"
    },
    "componentsSource": {
      "type": "string",
      "enum": [ "component", "local" ],
      "default": "component"
    },
    "release": {
      "type": "string",
      "enum": ["none", "release-it", "schematic-release"],
      "default": "release-it",
      "x-prompt": "Which release strategy do you want to use?"
    },
    "helmChart": {
      "type": "string",
      "description": "The name of the helm chart project path with namespace where the app version should be automatically updated"
    },
    "skipFormat": {
      "type": "boolean",
      "default": false
    },
    "overwrite": {
      "type": "boolean",
      "default": false,
      "description": "Whether to overwrite existing files"
    },
    "onlyPackages": {
      "type": "boolean",
      "description": "If true, the gitlab ci will be initialized exclusively for package development",
      "x-prompt": "Does this package only includes packages?",
      "default": false
    },
    "dte": {
      "type": "boolean",
      "description": "If true, the gitlab ci will be initialized with dte execution for the targets",
      "x-prompt": "Should nx workspace DTE be used?",
      "default": false
    },
    "parallel": {
      "type": "number",
      "default": 3,
      "description": "The number of parallel agents started for the nx workspace run tasks to run in the pipeline"
    },
    "angular": {
      "type": "boolean",
      "description": "If true, the gitlab ci will be initialized for a workspace with angular projects",
      "x-prompt": "Does this workspace include angular projects?",
      "default": false
    },
    "nest": {
      "type": "boolean",
      "description": "If true, the gitlab ci will be initialized for a workspace with nest projects",
      "x-prompt": "Does this workspace include nest projects?",
      "default": false
    }
  },
  "required": []
}
