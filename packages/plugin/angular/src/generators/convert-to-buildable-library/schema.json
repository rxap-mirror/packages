{
  "$schema": "http://json-schema.org/schema",
  "$id": "ConvertToBuildableLibrary",
  "title": "",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to convert to a buildable library.",
      "x-prompt": "What is the name of the project you want to convert?",
      "examples": [
        "my-lib"
      ]
    }
  },
  "required": ["project"]
}
