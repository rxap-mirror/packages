{
  "$schema": "http://json-schema.org/schema",
  "$id": "InitFeature",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the feature to initialize.",
      "examples": ["user-profile"]
    },
    "project": {
      "type": "string",
      "description": "The name of the project where the feature should be added.",
      "examples": ["my-app"]
    },
    "overwrite": {
      "type": "boolean",
      "description": "Whether to overwrite the feature if it already exists."
    },
    "skipFormat": {
      "type": "boolean",
      "default": false,
      "description": "Whether to skip formatting files with Prettier after initialization."
    },
    "apiStatusCheck": {
      "type": "boolean",
      "default": false,
      "description": "Whether to enable API status check for this feature."
    },
    "navigation": {
      "type": "object",
      "properties":{
        "label": {
          "type": "string",
          "description": "The label of the navigation item"
        },
        "icon": {
          "description": "The icon of the navigation item",
          "allOf": [
            {
              "type": "object",
              "properties":{
                "color": {
                  "type": "string",
                  "description": "The color of the icon"
                }
              }
            },
            {
              "oneOf": [
                {
                  "type": "object",
                  "description": "The icon is a material icon",
                  "properties": {
                    "icon": {
                      "type": "string",
                      "description": "The name of the icon"
                    }
                  },
                  "required": ["icon"]
                },
                {
                  "type": "object",
                  "description": "The icon is a svg icon",
                  "properties": {
                    "svgIcon": {
                      "type": "string",
                      "description": "The name of the icon"
                    }
                  },
                  "required": ["svgIcon"]
                }
              ]
            }
          ]
        }
      },
      "required": ["label"]
    }
  },
  "required": [
    "name",
    "project"
  ]
}
