{
  "$schema": "http://json-schema.org/schema",
  "$id": "ProjectTarget",
  "title": "",
  "type": "object",
  "properties": {
    "projects": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "A list of projects to update targets for.",
      "examples": [
        [
          "app1",
          "app2"
        ]
      ]
    },
    "overwrite": {
      "type": "boolean",
      "default": false,
      "description": "Whether to overwrite existing files.",
      "examples": [
        true,
        false
      ]
    },
    "cleanup": {
      "type": "boolean",
      "default": false,
      "description": "Remove if empty object for any configuration and the options property. After configuration cleanup.",
      "examples": [
        true,
        false
      ]
    },
    "simplify": {
      "type": "boolean",
      "default": false,
      "description": "If the property for the target is equal to the default from the nx.json then remove the property.",
      "examples": [
        true,
        false
      ]
    },
    "reorder": {
      "type": "boolean",
      "default": false,
      "description": "Reorder all target properties to be in alphabetic order.",
      "examples": [
        true,
        false
      ]
    },
    "project": {
      "type": "string",
      "description": "The name of the project to update targets for.",
      "examples": [
        "my-app"
      ]
    }
  },
  "required": []
}
