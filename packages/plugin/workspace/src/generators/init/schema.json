{
  "$schema": "http://json-schema.org/schema",
  "$id": "Init",
  "title": "",
  "type": "object",
  "properties": {
    "applications": {
      "type": "boolean",
      "description": "If true, the workspace will be initialized for application development"
    },
    "packages": {
      "type": "boolean",
      "description": "If true, the workspace will be initialized for package development"
    },
    "overwrite": {
      "type": "boolean",
      "default": false,
      "description": "Whether to overwrite existing files"
    }
  },
  "required": []
}
