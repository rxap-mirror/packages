{
  "$schema": "http://json-schema.org/schema",
  "$id": "Init",
  "title": "",
  "type": "object",
  "properties": {
    "packages": {
      "type": "boolean",
      "description": "If true, the workspace will be initialized for package development",
      "x-prompt": "Has this workspace publishable packages?"
    },
    "standalone": {
      "type": "boolean",
      "description": "If true, the workspace will be initialized for standalone development",
      "x-prompt": "Has this workspace a standalone project?"
    },
    "overwrite": {
      "type": "boolean",
      "default": false,
      "description": "Whether to overwrite existing files"
    },
    "skipFormat": {
      "type": "boolean",
      "default": false
    },
    "skipProjects": {
      "type": "boolean",
      "default": false,
      "description": "Whether to skip executing project specific initialization"
    },
    "skipLicense": {
      "type": "boolean",
      "default": false,
      "description": "Whether to skip adding a license file"
    },
    "license": {
      "type": "string",
      "default": "gpl",
      "enum": [
        "mit",
        "gpl"
      ]
    },
    "repositoryUrl": {
      "type": "string",
      "description": "The URL of the repository"
    },
    "prefix": {
      "type": "string",
      "description": "The prefix for the angular components"
    }
  },
  "required": []
}
