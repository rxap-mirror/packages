# n8n-nodes-litellm

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build n8n-nodes-litellm` to build the library.

## Running unit tests

Run `nx test n8n-nodes-litellm` to execute the unit tests via [Jest](https://jestjs.io).
