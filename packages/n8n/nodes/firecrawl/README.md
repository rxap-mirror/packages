# n8n-nodes-firecrawl

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build n8n-nodes-firecrawl` to build the library.

## Running unit tests

Run `nx test n8n-nodes-firecrawl` to execute the unit tests via [Jest](https://jestjs.io).
