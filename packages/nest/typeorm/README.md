# nest-typeorm

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nest-typeorm` to build the library.

## Running unit tests

Run `nx test nest-typeorm` to execute the unit tests via [Jest](https://jestjs.io).
