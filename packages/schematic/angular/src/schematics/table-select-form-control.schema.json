{
  "$schema": "http://json-schema.org/schema",
  "$id": "table-select-form-control",
  "allOf": [
    {
      "$ref": "#/definitions/formFieldFormControl"
    },
    {
      "type": "object",
      "properties": {
        "placeholder": {
          "type": "string"
        },
        "columnList": {
          "alias": "column",
          "type": "array",
          "items": {
            "description": "table column name",
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "table column name"
              },
              "title": {
                "type": "string",
                "description": "table column label"
              },
              "hasFilter": {
                "type": "boolean",
                "description": "Whether the column has a filter"
              },
              "kind": {
                "type": "string",
                "description": "The kind of data in the column"
              }
            }
          },
          "description": "List of table column names"
        },
        "title": {
          "type": "string",
          "description": "The title of the table select window"
        },
        "toDisplay": {
          "type": "object",
          "properties": {
            "property": {
              "$ref": "#/definitions/property"
            }
          }
        },
        "toValue": {
          "type": "object",
          "properties": {
            "property": {
              "$ref": "#/definitions/property"
            }
          }
        },
        "upstream": {
          "$ref": "#/definitions/upstream"
        },
        "resolver": {
          "type": "object",
          "properties": {
            "upstream": {
              "$ref": "#/definitions/upstream"
            }
          }
        }
      }
    }
  ],
  "definitions": {
    "formFieldFormControl": {
      "$ref": "./form-field-form-control.schema.json"
    },
    "property": {
      "$ref": "./property.schema.json"
    },
    "upstream": {
      "$ref": "./upstream.schema.json"
    }
  }
}
