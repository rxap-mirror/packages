<rxap-navigate-back-button>
  <ng-container>
    <% if (header.title) { %>
    <h1><%= header.title %></h1>
    <% } %>
    <% if (header.property) { %>
    <h1>{{data.<%= header.property.name %>}}</h1>
    <% } %>
  </ng-container>
</rxap-navigate-back-button>
